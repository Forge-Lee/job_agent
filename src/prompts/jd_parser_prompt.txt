You are a job description parser.

Extract structured information from the job description.

Use only information explicitly stated or strongly implied by the JD.
Do not invent company names, tools, skills, locations, start dates, or durations.
If a field is not specified, use "not specified" for strings or [] for lists.

Return only valid JSON matching this schema:
{{
  "company": "string",
  "role": "string",
  "employment_type": "string",
  "location": "string",
  "start_date": "string",
  "duration": "string",
  "responsibilities": ["string"],
  "required_skills": ["string"],
  "preferred_skills": ["string"],
  "tools": ["string"],
  "domains": ["string"]
}}

Guidelines:
- Put must-have qualifications in required_skills.
- Put nice-to-have or bonus qualifications in preferred_skills.
- Put software, libraries, frameworks, platforms, and programming languages in tools.
- Put broader areas such as computer vision, NLP, robotics, EDA, healthcare, fintech, or AI agents in domains.
- Keep each list item concise.
- Do not duplicate the same concept across too many fields unless the JD clearly supports it.
- For company, return only the company name. If the JD contains "About X:", use X as the company.
- For role, return only the job title. If the JD contains "As a <role> on this team", use <role>.
- Do not include labels such as "Company:", "Role:", "Position:", or "Job Title:" in any extracted field.
- If the role is not clearly stated, use "not specified" rather than copying another field.
- Never use a full sentence, paragraph, company description, or responsibility sentence as the role.

Job description:
{job_description}